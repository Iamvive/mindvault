{
  "name": "mindvault",
  "version": "1.0.0",
  "description": "MindVault - Personal Resource Ingestion Dashboard",
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "openskills": "^1.5.0"
  }
}
